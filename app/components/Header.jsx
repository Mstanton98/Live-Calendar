import Navbar from './Navbar';
import React from 'react';

const Header = React.createClass({

  render() {
    return (

      <div>
        <Navbar />
      </div>

    );
  }
});

export default Header;
