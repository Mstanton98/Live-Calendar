import Login from './Login';
import React from 'react';

const Navbar = React.createClass({

  render() {
    return (

      <div>
        <Login />
      </div>

    );
  }
});

export default Navbar;
